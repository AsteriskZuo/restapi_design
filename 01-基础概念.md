# REST API 基础概念

## 什么是 REST？

REST (Representational State Transfer) 是一种软件架构风格，由Roy Fielding在2000年提出。它定义了创建Web服务的一套约束条件。

### REST 的六大约束

1. **客户端-服务器架构 (Client-Server)**
   - 分离用户界面和数据存储的关注点
   - 提高跨平台的可移植性
   - 通过简化服务器组件来提高可伸缩性

2. **无状态 (Stateless)**
   - 每个请求必须包含处理该请求所需的所有信息
   - 服务器不能存储任何关于客户端会话的上下文信息
   - 提高可见性、可靠性和可伸缩性

3. **可缓存 (Cacheable)**
   - 响应中的数据必须隐式或显式地标记为可缓存或不可缓存
   - 提高网络效率

4. **统一接口 (Uniform Interface)**
   - 资源识别 (Resource Identification)
   - 通过表述来操作资源 (Resource Manipulation)
   - 自描述的消息 (Self-descriptive Messages)
   - 超媒体作为应用状态的引擎 (HATEOAS)

5. **分层系统 (Layered System)**
   - 允许通过限制组件的行为来简化整体系统架构
   - 支持负载均衡和共享缓存

6. **按需代码 (Code on Demand) - 可选**
   - 允许服务器向客户端发送可执行代码来扩展客户端功能

## 核心概念

### 资源 (Resource)

REST中的核心概念是资源，它是信息的抽象。

```
例如：
- 用户资源: /users
- 特定用户: /users/123
- 用户的文章: /users/123/posts
- 特定文章: /posts/456
```

### 资源标识符 (URI)

每个资源都有一个唯一的标识符，通常是URI。

**良好的URI设计原则：**
- 使用名词而不是动词
- 使用复数形式
- 使用小写字母
- 使用连字符而不是下划线
- 避免文件扩展名

### 表述 (Representation)

资源的具体表现形式，通常是JSON、XML等格式。

```json
{
  "id": 123,
  "name": "张三",
  "email": "zhangsan@example.com",
  "createdAt": "2024-01-01T00:00:00Z"
}
```

### HTTP 方法

REST使用标准的HTTP方法来操作资源：

- **GET**: 获取资源
- **POST**: 创建资源
- **PUT**: 更新整个资源
- **PATCH**: 部分更新资源
- **DELETE**: 删除资源

### 状态转移

通过HTTP方法对资源进行操作，实现应用状态的转移。

## RESTful API 的特点

### 优点

1. **简单性**: 基于HTTP协议，易于理解和实现
2. **可扩展性**: 无状态特性使得系统易于扩展
3. **可缓存性**: 支持HTTP缓存机制
4. **统一接口**: 标准化的操作方式
5. **平台无关性**: 可以在任何支持HTTP的平台上使用

### 缺点

1. **过度获取**: 可能返回不需要的数据
2. **获取不足**: 可能需要多次请求才能获取完整数据
3. **缓存复杂性**: 复杂查询的缓存管理困难
4. **实时性限制**: 不适合实时性要求很高的应用

## 与其他架构风格的比较

### REST vs SOAP

| 特性 | REST | SOAP |
|------|------|------|
| 协议 | HTTP | HTTP/HTTPS/SMTP等 |
| 数据格式 | JSON/XML | XML |
| 复杂性 | 简单 | 复杂 |
| 性能 | 较好 | 较差 |
| 缓存 | 支持 | 有限支持 |

### REST vs GraphQL

| 特性 | REST | GraphQL |
|------|------|---------|
| 数据获取 | 多个端点 | 单一端点 |
| 查询灵活性 | 固定 | 灵活 |
| 学习曲线 | 平缓 | 陡峭 |
| 缓存 | 简单 | 复杂 |
| 生态系统 | 成熟 | 新兴 |

## 成熟度模型 (Richardson Maturity Model)

Leonard Richardson提出了REST成熟度模型，分为4个级别：

### Level 0: The Swamp of POX (Plain Old XML)
- 使用HTTP作为传输协议
- 只使用一个URI和一个HTTP方法

### Level 1: Resources
- 引入资源概念
- 多个URI，但仍然只使用一个HTTP方法

### Level 2: HTTP Verbs
- 正确使用HTTP方法
- 使用HTTP状态码

### Level 3: Hypermedia Controls (HATEOAS)
- 在响应中包含相关链接
- 客户端通过超媒体发现可用的操作

大多数所谓的"RESTful"API实际上处于Level 2，真正的REST应该达到Level 3。 