# 响应格式和错误处理规范

## 通用响应格式

### 基本原则

1. **一致性**: 所有API使用统一的响应格式
2. **可预测性**: 客户端能够预期响应的结构
3. **信息充足**: 提供足够的信息供客户端处理
4. **向后兼容**: 新增字段不破坏现有客户端

### 成功响应格式

#### 单一资源响应

```json
{
  "data": {
    "id": 123,
    "name": "张三",
    "email": "zhangsan@example.com",
    "createdAt": "2024-01-01T12:00:00Z",
    "updatedAt": "2024-01-01T12:00:00Z"
  },
  "meta": {
    "timestamp": "2024-01-01T12:00:00Z",
    "version": "v1"
  }
}
```

#### 列表资源响应

```json
{
  "data": [
    {
      "id": 123,
      "name": "张三",
      "email": "zhangsan@example.com"
    },
    {
      "id": 124,
      "name": "李四",
      "email": "lisi@example.com"
    }
  ],
  "meta": {
    "pagination": {
      "page": 1,
      "limit": 10,
      "total": 50,
      "totalPages": 5
    },
    "timestamp": "2024-01-01T12:00:00Z",
    "version": "v1"
  }
}
```

#### 空数据响应

```json
{
  "data": [],
  "meta": {
    "pagination": {
      "page": 1,
      "limit": 10,
      "total": 0,
      "totalPages": 0
    },
    "timestamp": "2024-01-01T12:00:00Z",
    "version": "v1"
  }
}
```

### 错误响应格式

#### 基本错误格式

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "请求参数验证失败",
    "details": {
      "field": "email",
      "value": "invalid-email",
      "reason": "邮箱格式不正确"
    },
    "timestamp": "2024-01-01T12:00:00Z",
    "requestId": "req-123456789"
  }
}
```

#### 多重错误格式

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "请求包含多个验证错误",
    "details": [
      {
        "field": "email",
        "value": "invalid-email",
        "code": "INVALID_FORMAT",
        "message": "邮箱格式不正确"
      },
      {
        "field": "age",
        "value": -5,
        "code": "OUT_OF_RANGE",
        "message": "年龄必须大于0"
      }
    ],
    "timestamp": "2024-01-01T12:00:00Z",
    "requestId": "req-123456789"
  }
}
```

## 错误代码规范

### 错误代码结构

使用分层的错误代码结构：`{CATEGORY}_{SPECIFIC_ERROR}`

```
认证相关: AUTH_*
- AUTH_TOKEN_MISSING
- AUTH_TOKEN_INVALID
- AUTH_TOKEN_EXPIRED

权限相关: PERMISSION_*
- PERMISSION_DENIED
- PERMISSION_INSUFFICIENT

验证相关: VALIDATION_*
- VALIDATION_REQUIRED_FIELD
- VALIDATION_INVALID_FORMAT
- VALIDATION_OUT_OF_RANGE

业务逻辑: BUSINESS_*
- BUSINESS_DUPLICATE_EMAIL
- BUSINESS_INSUFFICIENT_BALANCE
- BUSINESS_OPERATION_NOT_ALLOWED

系统错误: SYSTEM_*
- SYSTEM_DATABASE_ERROR
- SYSTEM_EXTERNAL_SERVICE_ERROR
- SYSTEM_TIMEOUT
```

### 常见错误码定义

#### 400 - 客户端错误

```json
{
  "error": {
    "code": "VALIDATION_REQUIRED_FIELD",
    "message": "缺少必填字段",
    "details": {
      "field": "email",
      "message": "邮箱字段是必填的"
    }
  }
}
```

```json
{
  "error": {
    "code": "VALIDATION_INVALID_FORMAT",
    "message": "字段格式不正确",
    "details": {
      "field": "email",
      "value": "not-an-email",
      "expectedFormat": "email",
      "example": "user@example.com"
    }
  }
}
```

#### 401 - 认证错误

```json
{
  "error": {
    "code": "AUTH_TOKEN_MISSING",
    "message": "缺少访问令牌",
    "details": {
      "hint": "请在Authorization header中提供Bearer token"
    }
  }
}
```

```json
{
  "error": {
    "code": "AUTH_TOKEN_EXPIRED",
    "message": "访问令牌已过期",
    "details": {
      "expiredAt": "2024-01-01T10:00:00Z",
      "hint": "请使用refresh token获取新的访问令牌"
    }
  }
}
```

#### 403 - 权限错误

```json
{
  "error": {
    "code": "PERMISSION_DENIED",
    "message": "权限不足",
    "details": {
      "resource": "users",
      "action": "delete",
      "requiredRole": "admin",
      "currentRole": "user"
    }
  }
}
```

#### 404 - 资源不存在

```json
{
  "error": {
    "code": "RESOURCE_NOT_FOUND",
    "message": "请求的资源不存在",
    "details": {
      "resource": "user",
      "id": "999",
      "hint": "请检查用户ID是否正确"
    }
  }
}
```

#### 409 - 冲突错误

```json
{
  "error": {
    "code": "BUSINESS_DUPLICATE_EMAIL",
    "message": "邮箱地址已被使用",
    "details": {
      "field": "email",
      "value": "zhangsan@example.com",
      "conflictsWith": "user-456"
    }
  }
}
```

#### 422 - 业务逻辑错误

```json
{
  "error": {
    "code": "BUSINESS_INSUFFICIENT_BALANCE",
    "message": "账户余额不足",
    "details": {
      "currentBalance": 50.00,
      "requiredAmount": 100.00,
      "currency": "CNY"
    }
  }
}
```

#### 429 - 频率限制

```json
{
  "error": {
    "code": "RATE_LIMIT_EXCEEDED",
    "message": "请求频率超过限制",
    "details": {
      "limit": 100,
      "remaining": 0,
      "resetTime": "2024-01-01T13:00:00Z",
      "retryAfter": 3600
    }
  }
}
```

#### 500 - 服务器错误

```json
{
  "error": {
    "code": "SYSTEM_INTERNAL_ERROR",
    "message": "服务器内部错误",
    "details": {
      "requestId": "req-123456789",
      "hint": "请联系技术支持并提供请求ID"
    }
  }
}
```

## 响应头规范

### 通用响应头

```http
Content-Type: application/json; charset=utf-8
X-Request-ID: req-123456789
X-Response-Time: 123ms
X-API-Version: v1
Cache-Control: no-cache
```

### 分页响应头

```http
X-Pagination-Page: 1
X-Pagination-Limit: 10
X-Pagination-Total: 50
X-Pagination-Total-Pages: 5
Link: <https://api.example.com/users?page=2>; rel="next",
      <https://api.example.com/users?page=5>; rel="last"
```

### 错误相关响应头

```http
# 认证错误
WWW-Authenticate: Bearer realm="api"

# 频率限制
X-RateLimit-Limit: 100
X-RateLimit-Remaining: 0
X-RateLimit-Reset: 1640995200
Retry-After: 3600

# 服务不可用
Retry-After: 60
```

## 字段命名规范

### 命名风格

使用 `camelCase` 风格：

```json
✅ 正确：
{
  "userId": 123,
  "firstName": "张",
  "lastName": "三",
  "createdAt": "2024-01-01T12:00:00Z",
  "isActive": true
}

❌ 错误：
{
  "user_id": 123,
  "first_name": "张",
  "last_name": "三",
  "created_at": "2024-01-01T12:00:00Z",
  "is_active": true
}
```

### 布尔字段

使用 `is` 或 `has` 前缀：

```json
{
  "isActive": true,
  "isDeleted": false,
  "hasAvatar": true,
  "canEdit": false
}
```

### 时间字段

统一使用 ISO 8601 格式：

```json
{
  "createdAt": "2024-01-01T12:00:00Z",
  "updatedAt": "2024-01-01T12:30:00Z",
  "deletedAt": null,
  "publishedAt": "2024-01-01T10:00:00+08:00"
}
```

### 枚举字段

使用大写下划线格式：

```json
{
  "status": "ACTIVE",
  "userType": "PREMIUM_USER",
  "orderStatus": "PENDING_PAYMENT"
}
```

## 数据类型规范

### 字符串

```json
{
  "name": "张三",
  "email": "zhangsan@example.com",
  "description": "这是一个描述文本"
}
```

### 数字

```json
{
  "id": 123,
  "age": 25,
  "price": 99.99,
  "score": 85.5
}
```

### 布尔值

```json
{
  "isActive": true,
  "isDeleted": false
}
```

### 数组

```json
{
  "tags": ["技术", "编程", "REST"],
  "permissions": ["read", "write", "delete"],
  "scores": [85, 90, 78]
}
```

### 对象

```json
{
  "address": {
    "street": "中关村大街1号",
    "city": "北京",
    "zipCode": "100080",
    "country": "中国"
  }
}
```

### null 值处理

```json
{
  "avatar": null,          // 明确表示没有头像
  "description": "",       // 空字符串表示有该字段但内容为空
  "deletedAt": null        // null表示未删除
}
```

## 分页响应规范

### 标准分页格式

```json
{
  "data": [...],
  "meta": {
    "pagination": {
      "page": 1,
      "limit": 10,
      "total": 100,
      "totalPages": 10,
      "hasNext": true,
      "hasPrev": false
    }
  },
  "links": {
    "self": "https://api.example.com/users?page=1&limit=10",
    "first": "https://api.example.com/users?page=1&limit=10",
    "prev": null,
    "next": "https://api.example.com/users?page=2&limit=10",
    "last": "https://api.example.com/users?page=10&limit=10"
  }
}
```

### 游标分页格式

```json
{
  "data": [...],
  "meta": {
    "pagination": {
      "limit": 10,
      "hasNext": true,
      "hasPrev": false
    }
  },
  "links": {
    "self": "https://api.example.com/users?cursor=abc123&limit=10",
    "next": "https://api.example.com/users?cursor=def456&limit=10",
    "prev": null
  }
}
```

## 国际化支持

### 多语言错误消息

```json
{
  "error": {
    "code": "VALIDATION_REQUIRED_FIELD",
    "message": "Required field is missing",
    "localizedMessage": {
      "zh-CN": "缺少必填字段",
      "en-US": "Required field is missing"
    },
    "details": {
      "field": "email"
    }
  }
}
```

### 请求头

```http
Accept-Language: zh-CN,zh;q=0.9,en;q=0.8
Content-Language: zh-CN
```

## 性能考虑

### 字段选择

支持通过查询参数选择返回字段：

```
GET /api/v1/users?fields=id,name,email
```

```json
{
  "data": [
    {
      "id": 123,
      "name": "张三",
      "email": "zhangsan@example.com"
    }
  ]
}
```

### 数据压缩

```http
Accept-Encoding: gzip, deflate
Content-Encoding: gzip
```

### 缓存控制

```http
Cache-Control: public, max-age=3600
ETag: "abc123"
Last-Modified: Tue, 01 Jan 2024 12:00:00 GMT
```

## 版本兼容性

### 向后兼容原则

1. **新增字段**: 可以添加新字段
2. **字段类型**: 不能改变现有字段类型
3. **字段删除**: 不能删除现有字段（可以标记为deprecated）
4. **枚举值**: 可以添加新的枚举值，不能删除现有值

### 版本演进示例

**v1 响应**:
```json
{
  "data": {
    "id": 123,
    "name": "张三",
    "email": "zhangsan@example.com"
  }
}
```

**v2 响应 (向后兼容)**:
```json
{
  "data": {
    "id": 123,
    "name": "张三",
    "email": "zhangsan@example.com",
    "avatar": "https://example.com/avatar.jpg",  // 新增字段
    "profile": {                                 // 新增嵌套对象
      "bio": "软件工程师",
      "location": "北京"
    }
  }
}
```

## 测试和验证

### 响应格式验证清单

- [ ] 使用统一的成功响应格式
- [ ] 使用统一的错误响应格式
- [ ] 错误代码遵循命名规范
- [ ] 字段命名使用camelCase
- [ ] 时间字段使用ISO 8601格式
- [ ] 包含必要的元数据信息
- [ ] 支持分页的接口返回分页信息
- [ ] 错误响应包含足够的调试信息
- [ ] 响应头包含必要的元信息
- [ ] 支持国际化的接口返回多语言消息

### 自动化测试

```javascript
// 响应格式验证示例
function validateSuccessResponse(response) {
  assert.exists(response.data);
  assert.exists(response.meta);
  assert.exists(response.meta.timestamp);
  assert.isString(response.meta.version);
}

function validateErrorResponse(response) {
  assert.exists(response.error);
  assert.exists(response.error.code);
  assert.exists(response.error.message);
  assert.exists(response.error.timestamp);
  assert.exists(response.error.requestId);
}
``` 